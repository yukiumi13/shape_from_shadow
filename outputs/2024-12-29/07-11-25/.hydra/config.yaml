dataset:
  name: single_image
  data_path: shadow.png
model:
  shape:
    name: shape2vecset
    latent_num: 512
    latent_dim: 8
    occ_resolution: 128
    pretrained_weight: /home/yangli/sfs/checkpoints/pretrained/ae_kl_d512_m512_l8.pth
  render:
    name: simple_shadow_render
    rays_sample: -1
    sample_per_ray: 128
    ground_aabb:
    - -1.0
    - -1.0
    - 1.0
    - 1.0
    ground_res: 128
loss:
  name: cross_entropy
  weight: 1.0
wandb:
  project: shape_from_shadow
  entity: aurora_ly
  name: placeholder
  mode: disabled
  id: null
mode: train
data_loader:
  train:
    num_workers: 1
    persistent_workers: true
    batch_size: 1
    seed: 1234
  test:
    num_workers: 1
    persistent_workers: false
    batch_size: 1
    seed: 2345
  val:
    num_workers: 1
    persistent_workers: true
    batch_size: 1
    seed: 3456
checkpointing:
  load: null
  every_n_train_steps: 1000
  save_top_k: -1
  resume: true
train:
  depth_mode: null
  print_log_every_n_steps: 1
seed: 111123
trainer:
  max_steps: -1
  val_check_interval: 0.5
  gradient_clip_val: 0.5
  num_sanity_val_steps: 2
  num_nodes: 1
root_dir: outputs
output_dir: local
